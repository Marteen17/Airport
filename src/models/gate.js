class Gate {
  constructor(gateNumber, gateStatus, assignedPlane) {
    this.gateNumber = gateNumber;
    this.gateStatus = gateStatus;
    this.assignedPlane = assignedPlane;
  }
}

module.exports = Gate;

  